# Phase 4 overlay. Apply this only after creating Probe Two in the dashboard
# and replacing the placeholder below with its newly generated key.

probes:
  two:
    name: "External-Probe-Node2"
    description: "aa"
    enabled: true
    monitoringWorkers: 3
    monitorFetchLimit: 10
    key: "e80ad61a-77ca-4dad-9545-1dae754c5771"
    replicaCount: 1
    ports:
      http: 3874
    nodeSelector:
      app: oneuptime-probe
